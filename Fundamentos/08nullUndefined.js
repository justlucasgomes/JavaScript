/*
📌 undefined → Algo que não foi definido
📌 null → Algo que foi definido como vazio de propósito*/

let usuario; // Não foi definido, então é undefined
console.log(usuario); // undefined

usuario = null; // Agora o usuário "existe", mas está vazio
console.log(usuario); // null